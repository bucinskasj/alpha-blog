<h1 class="text-center mt-4">Alpha bloggers</h1>
<div class= "container">
  <% @users.each do |user| %>
    <div class="row justify-content-md-center">
        <div class="col-8 mt-4">
          <div class="card text-center">
            <div class="card-header">
              <%= link_to user.username, user_path(user) %>
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= link_to gravatar_for(user, size: 150), user %></h5>
              <p class="card-text"><%= pluralize(user.articles.count, "article") %></p>
              <%= link_to "View profile", user, class: "btn btn-outline-success" %> </a>
              <%= link_to "Edit profile", edit_user_path(user), class: "btn btn-outline-info"%> </a>
            </div>
            <div class="card-footer text-muted">
              <small> Jonas <%= time_ago_in_words(user.created_at) %> ago</small>
            </div>
          </div>
        </div>
    </div>
  <% end %>
</div>